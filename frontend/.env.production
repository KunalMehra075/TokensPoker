# Production build config (used by `vite build`, which runs in production mode).
# Not secret. Overrides the localhost value in .env for prod. Cloudflare Pages
# env vars still take precedence over this if set in the dashboard.
VITE_API_URL=https://api.freetokenspoker.com
